import styled from 'styled-components';

const Wrapper = styled.div`
  width: 100%;
  height: 52%;

  border-bottom: 1px solid var(--colour-mobile-border);
  margin: 0 20px;
`;

export default Wrapper;
